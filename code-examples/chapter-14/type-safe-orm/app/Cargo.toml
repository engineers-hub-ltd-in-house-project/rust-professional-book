[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
query-macro = { path = "../query-macro" }
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.6", features = ["runtime-tokio-native-tls", "sqlite"] }
anyhow = "1.0"
